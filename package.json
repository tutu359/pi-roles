{
  "name": "pi-roles",
  "version": "0.3.0",
  "description": "Pi 角色模板系统：一个角色 = roles/<name>.md 一个文件，放目录即自动出现在 /role 菜单。replace 替换系统提示词（主会话换角色）/ append 追加（委派子代理带角色）。实时拒绝拦截随角色模式联动。从 pi-session-patcher fork 独立。",
  "type": "module",
  "license": "MIT",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --experimental-strip-types test/detector.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.86.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}
