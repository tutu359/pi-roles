{
  "name": "pi-roles",
  "version": "0.4.0",
  "description": "Pi 角色模板系统：一个角色 = roles/<name>.md 一个文件。/role 菜单���外层：追加/替换模式 + 自动拦截 → 角色子菜单）与启动旗标（--role <name>）两条通道；append 追加（默认，保留原生剧本）/ replace 替换系统提示词；实时拒绝拦截随角色联动。从 pi-session-patcher fork 独立。",
  "type": "module",
  "license": "MIT",
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "scripts": {
    "test": "node --experimental-strip-types test/detector.test.ts && node --experimental-strip-types test/state.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.86.0",
    "@types/node": "^22.0.0",
    "typescript": "^5.5.0"
  }
}